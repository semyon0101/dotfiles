# timeout 600 'brightnessctl -s set 10%' resume 'brightnessctl -r'

# timeout 720 'systemctl suspend'

before-sleep 'hyprlock-login.sh & sleep 0.5 && niri msg action power-off-monitors'
after-resume 'niri msg action power-on-monitors'

lock 'hyprlock-login.sh'

